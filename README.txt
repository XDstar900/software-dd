Open up the python file in vscode.
Open up the vscode terminal and paste in this command: pip install colorama
Now you can run it. Although i would recommend you run it directly from the file, instead of vscode just to prevent being overwhelmed by the text.